import 'package:flutter/material.dart';

/// Paleta oficial del proyecto (DOCS/context.md, Parte A, sección 3).
/// No agregues colores fuera de esta paleta — ver Parte A, sección 4.
class AppColors {
  AppColors._();

  static const navy = Color(0xFF2F4156);
  static const teal = Color(0xFF567C8D);
  static const skyBlue = Color(0xFFC8D9E6);
  static const beige = Color(0xFFF5EFEB);
  static const white = Color(0xFFFFFFFF);
}
